version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: redis-persistent
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    # This command enables Append Only File (AOF) persistence
    command: redis-server --appendonly yes

volumes:
  redis_data: